[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
]

[tool.uv.sources]
shared = { workspace = true }
