[project]
name = "census-demo"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["src/shared", "src/etl", "src/api", "src/app"]

[dependency-groups]
dev = ["pytest>=8.2", "httpx>=0.27"]
